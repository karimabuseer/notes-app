document.addEventListener("DOMContentLoaded", () => {
  let button = document.getElementById("createNote")
  let list = document.getElementById("notesList")

  button.addEventListener("click", (event) => {
    event.preventDefault()
    
  })
  
})
